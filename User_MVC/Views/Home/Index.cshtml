@{
    ViewData["Title"] = "Home Page";
    Layout = "_Layout";
}

@model List<Sabzavot>
<table class="table table-dark table-striped">
    <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Name</th>
            <th scope="col">Price</th>
        </tr>
    </thead>
    <tbody>
        @if (Model == null)
        {
            <h2>Is a null reference</h2>
        }
        else
        {
            @foreach (var meva in Model)
            {
                <tr>
                    <th scope="row">@meva.Id</th>
                    <td>@meva.Name</td>
                    <td>@meva.Price</td>
                </tr>
            }
        }

    </tbody>
</table>
